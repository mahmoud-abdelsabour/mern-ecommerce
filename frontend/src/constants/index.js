export const PAGINATION = {
    DEFAULT_PAGE: 1,
    DEFAULT_LIMIT: 12,
}

export const API = {
    TIMEOUT: 30000,
    RETRY_ATTEMPTS: 3,
}

export const CART = {
    MAX_QUANTITY: 99,
}

export const UI = {
    DEBOUNCE_DELAY: 300,
    TOAST_DURATION: 3000,
}